extend type Mutation {
    createThread(input: CreateThreadInput!, messageInput: CreateMessageInput!): Thread!
    updateThread(id: ID!, input: UpdateThreadInput!): Thread!
    deleteThread(id: ID!):ID!
}

extend type Query {
    thread(id:ID!): Thread!
}