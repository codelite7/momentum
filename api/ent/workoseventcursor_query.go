// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/codelite7/momentum/api/ent/predicate"
	"github.com/codelite7/momentum/api/ent/workoseventcursor"
)

// WorkosEventCursorQuery is the builder for querying WorkosEventCursor entities.
type WorkosEventCursorQuery struct {
	config
	ctx        *QueryContext
	order      []workoseventcursor.OrderOption
	inters     []Interceptor
	predicates []predicate.WorkosEventCursor
	loadTotal  []func(context.Context, []*WorkosEventCursor) error
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the WorkosEventCursorQuery builder.
func (wecq *WorkosEventCursorQuery) Where(ps ...predicate.WorkosEventCursor) *WorkosEventCursorQuery {
	wecq.predicates = append(wecq.predicates, ps...)
	return wecq
}

// Limit the number of records to be returned by this query.
func (wecq *WorkosEventCursorQuery) Limit(limit int) *WorkosEventCursorQuery {
	wecq.ctx.Limit = &limit
	return wecq
}

// Offset to start from.
func (wecq *WorkosEventCursorQuery) Offset(offset int) *WorkosEventCursorQuery {
	wecq.ctx.Offset = &offset
	return wecq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (wecq *WorkosEventCursorQuery) Unique(unique bool) *WorkosEventCursorQuery {
	wecq.ctx.Unique = &unique
	return wecq
}

// Order specifies how the records should be ordered.
func (wecq *WorkosEventCursorQuery) Order(o ...workoseventcursor.OrderOption) *WorkosEventCursorQuery {
	wecq.order = append(wecq.order, o...)
	return wecq
}

// First returns the first WorkosEventCursor entity from the query.
// Returns a *NotFoundError when no WorkosEventCursor was found.
func (wecq *WorkosEventCursorQuery) First(ctx context.Context) (*WorkosEventCursor, error) {
	nodes, err := wecq.Limit(1).All(setContextOp(ctx, wecq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{workoseventcursor.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (wecq *WorkosEventCursorQuery) FirstX(ctx context.Context) *WorkosEventCursor {
	node, err := wecq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first WorkosEventCursor ID from the query.
// Returns a *NotFoundError when no WorkosEventCursor ID was found.
func (wecq *WorkosEventCursorQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = wecq.Limit(1).IDs(setContextOp(ctx, wecq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{workoseventcursor.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (wecq *WorkosEventCursorQuery) FirstIDX(ctx context.Context) int {
	id, err := wecq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single WorkosEventCursor entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one WorkosEventCursor entity is found.
// Returns a *NotFoundError when no WorkosEventCursor entities are found.
func (wecq *WorkosEventCursorQuery) Only(ctx context.Context) (*WorkosEventCursor, error) {
	nodes, err := wecq.Limit(2).All(setContextOp(ctx, wecq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{workoseventcursor.Label}
	default:
		return nil, &NotSingularError{workoseventcursor.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (wecq *WorkosEventCursorQuery) OnlyX(ctx context.Context) *WorkosEventCursor {
	node, err := wecq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only WorkosEventCursor ID in the query.
// Returns a *NotSingularError when more than one WorkosEventCursor ID is found.
// Returns a *NotFoundError when no entities are found.
func (wecq *WorkosEventCursorQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = wecq.Limit(2).IDs(setContextOp(ctx, wecq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{workoseventcursor.Label}
	default:
		err = &NotSingularError{workoseventcursor.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (wecq *WorkosEventCursorQuery) OnlyIDX(ctx context.Context) int {
	id, err := wecq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of WorkosEventCursors.
func (wecq *WorkosEventCursorQuery) All(ctx context.Context) ([]*WorkosEventCursor, error) {
	ctx = setContextOp(ctx, wecq.ctx, "All")
	if err := wecq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*WorkosEventCursor, *WorkosEventCursorQuery]()
	return withInterceptors[[]*WorkosEventCursor](ctx, wecq, qr, wecq.inters)
}

// AllX is like All, but panics if an error occurs.
func (wecq *WorkosEventCursorQuery) AllX(ctx context.Context) []*WorkosEventCursor {
	nodes, err := wecq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of WorkosEventCursor IDs.
func (wecq *WorkosEventCursorQuery) IDs(ctx context.Context) (ids []int, err error) {
	if wecq.ctx.Unique == nil && wecq.path != nil {
		wecq.Unique(true)
	}
	ctx = setContextOp(ctx, wecq.ctx, "IDs")
	if err = wecq.Select(workoseventcursor.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (wecq *WorkosEventCursorQuery) IDsX(ctx context.Context) []int {
	ids, err := wecq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (wecq *WorkosEventCursorQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, wecq.ctx, "Count")
	if err := wecq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, wecq, querierCount[*WorkosEventCursorQuery](), wecq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (wecq *WorkosEventCursorQuery) CountX(ctx context.Context) int {
	count, err := wecq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (wecq *WorkosEventCursorQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, wecq.ctx, "Exist")
	switch _, err := wecq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (wecq *WorkosEventCursorQuery) ExistX(ctx context.Context) bool {
	exist, err := wecq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the WorkosEventCursorQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (wecq *WorkosEventCursorQuery) Clone() *WorkosEventCursorQuery {
	if wecq == nil {
		return nil
	}
	return &WorkosEventCursorQuery{
		config:     wecq.config,
		ctx:        wecq.ctx.Clone(),
		order:      append([]workoseventcursor.OrderOption{}, wecq.order...),
		inters:     append([]Interceptor{}, wecq.inters...),
		predicates: append([]predicate.WorkosEventCursor{}, wecq.predicates...),
		// clone intermediate query.
		sql:  wecq.sql.Clone(),
		path: wecq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UserCreatedCursor string `json:"user_created_cursor,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.WorkosEventCursor.Query().
//		GroupBy(workoseventcursor.FieldUserCreatedCursor).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (wecq *WorkosEventCursorQuery) GroupBy(field string, fields ...string) *WorkosEventCursorGroupBy {
	wecq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &WorkosEventCursorGroupBy{build: wecq}
	grbuild.flds = &wecq.ctx.Fields
	grbuild.label = workoseventcursor.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UserCreatedCursor string `json:"user_created_cursor,omitempty"`
//	}
//
//	client.WorkosEventCursor.Query().
//		Select(workoseventcursor.FieldUserCreatedCursor).
//		Scan(ctx, &v)
func (wecq *WorkosEventCursorQuery) Select(fields ...string) *WorkosEventCursorSelect {
	wecq.ctx.Fields = append(wecq.ctx.Fields, fields...)
	sbuild := &WorkosEventCursorSelect{WorkosEventCursorQuery: wecq}
	sbuild.label = workoseventcursor.Label
	sbuild.flds, sbuild.scan = &wecq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a WorkosEventCursorSelect configured with the given aggregations.
func (wecq *WorkosEventCursorQuery) Aggregate(fns ...AggregateFunc) *WorkosEventCursorSelect {
	return wecq.Select().Aggregate(fns...)
}

func (wecq *WorkosEventCursorQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range wecq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, wecq); err != nil {
				return err
			}
		}
	}
	for _, f := range wecq.ctx.Fields {
		if !workoseventcursor.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if wecq.path != nil {
		prev, err := wecq.path(ctx)
		if err != nil {
			return err
		}
		wecq.sql = prev
	}
	return nil
}

func (wecq *WorkosEventCursorQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*WorkosEventCursor, error) {
	var (
		nodes = []*WorkosEventCursor{}
		_spec = wecq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*WorkosEventCursor).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &WorkosEventCursor{config: wecq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(wecq.modifiers) > 0 {
		_spec.Modifiers = wecq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, wecq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	for i := range wecq.loadTotal {
		if err := wecq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (wecq *WorkosEventCursorQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := wecq.querySpec()
	if len(wecq.modifiers) > 0 {
		_spec.Modifiers = wecq.modifiers
	}
	_spec.Node.Columns = wecq.ctx.Fields
	if len(wecq.ctx.Fields) > 0 {
		_spec.Unique = wecq.ctx.Unique != nil && *wecq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, wecq.driver, _spec)
}

func (wecq *WorkosEventCursorQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(workoseventcursor.Table, workoseventcursor.Columns, sqlgraph.NewFieldSpec(workoseventcursor.FieldID, field.TypeInt))
	_spec.From = wecq.sql
	if unique := wecq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if wecq.path != nil {
		_spec.Unique = true
	}
	if fields := wecq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, workoseventcursor.FieldID)
		for i := range fields {
			if fields[i] != workoseventcursor.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := wecq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := wecq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := wecq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := wecq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (wecq *WorkosEventCursorQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(wecq.driver.Dialect())
	t1 := builder.Table(workoseventcursor.Table)
	columns := wecq.ctx.Fields
	if len(columns) == 0 {
		columns = workoseventcursor.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if wecq.sql != nil {
		selector = wecq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if wecq.ctx.Unique != nil && *wecq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range wecq.modifiers {
		m(selector)
	}
	for _, p := range wecq.predicates {
		p(selector)
	}
	for _, p := range wecq.order {
		p(selector)
	}
	if offset := wecq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := wecq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (wecq *WorkosEventCursorQuery) ForUpdate(opts ...sql.LockOption) *WorkosEventCursorQuery {
	if wecq.driver.Dialect() == dialect.Postgres {
		wecq.Unique(false)
	}
	wecq.modifiers = append(wecq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return wecq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (wecq *WorkosEventCursorQuery) ForShare(opts ...sql.LockOption) *WorkosEventCursorQuery {
	if wecq.driver.Dialect() == dialect.Postgres {
		wecq.Unique(false)
	}
	wecq.modifiers = append(wecq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return wecq
}

// WorkosEventCursorGroupBy is the group-by builder for WorkosEventCursor entities.
type WorkosEventCursorGroupBy struct {
	selector
	build *WorkosEventCursorQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (wecgb *WorkosEventCursorGroupBy) Aggregate(fns ...AggregateFunc) *WorkosEventCursorGroupBy {
	wecgb.fns = append(wecgb.fns, fns...)
	return wecgb
}

// Scan applies the selector query and scans the result into the given value.
func (wecgb *WorkosEventCursorGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wecgb.build.ctx, "GroupBy")
	if err := wecgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WorkosEventCursorQuery, *WorkosEventCursorGroupBy](ctx, wecgb.build, wecgb, wecgb.build.inters, v)
}

func (wecgb *WorkosEventCursorGroupBy) sqlScan(ctx context.Context, root *WorkosEventCursorQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(wecgb.fns))
	for _, fn := range wecgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*wecgb.flds)+len(wecgb.fns))
		for _, f := range *wecgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*wecgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wecgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// WorkosEventCursorSelect is the builder for selecting fields of WorkosEventCursor entities.
type WorkosEventCursorSelect struct {
	*WorkosEventCursorQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (wecs *WorkosEventCursorSelect) Aggregate(fns ...AggregateFunc) *WorkosEventCursorSelect {
	wecs.fns = append(wecs.fns, fns...)
	return wecs
}

// Scan applies the selector query and scans the result into the given value.
func (wecs *WorkosEventCursorSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wecs.ctx, "Select")
	if err := wecs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WorkosEventCursorQuery, *WorkosEventCursorSelect](ctx, wecs.WorkosEventCursorQuery, wecs, wecs.inters, v)
}

func (wecs *WorkosEventCursorSelect) sqlScan(ctx context.Context, root *WorkosEventCursorQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(wecs.fns))
	for _, fn := range wecs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*wecs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wecs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
